name: insight
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  sqflite: ^2.3.0
  path: ^1.8.3
  image_picker: ^1.0.7
  image: ^4.1.3
  fl_chart: ^0.66.0
  http: ^1.2.0
  file_picker: 11.0.2  # <--- Pasang ini lagi bro         # Rest API Client

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
